{
  "name": "markdown-negotiation-sandbox",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for Markdown Negotiation for Agents WordPress sandbox",
  "scripts": {
    "test": "playwright test",
    "test:setup": "playwright test --project=setup",
    "test:e2e": "playwright test --project=e2e",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
